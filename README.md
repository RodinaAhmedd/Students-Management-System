# Students-Management-System
A simple JavaScript project that manages student grades, calculates average, highest and lowest scores, and allows searching for a student by name. The script demonstrates basic operations using arrays, loops, conditions, and user input in the browser console.
